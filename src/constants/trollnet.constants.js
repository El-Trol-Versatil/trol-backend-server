const CreationStatus = {
  FAILED: -1,
  CREATED: 100,
};

const TrainingStatus = {
  FAILED: -1,
  UNTRAINED: 0,
  TRAINED: 100,
};


const trollnetConstants = {
  CreationStatus,
  TrainingStatus,
};

module.exports = trollnetConstants;